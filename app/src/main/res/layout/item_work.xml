<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Фоновое изображение -->
        <ImageView
            android:id="@+id/imageViewWork"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:scaleType="centerCrop"
            android:adjustViewBounds="true"
            android:src="@drawable/ic_launcher_background" />

        <!-- Градиентное затемнение снизу -->
        <View
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:layout_alignBottom="@id/imageViewWork"
            android:background="@drawable/gradient_scrim" />

        <!-- Название авто -->
        <TextView
            android:id="@+id/textViewTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignBottom="@id/imageViewWork"
            android:layout_marginStart="16dp"
            android:layout_marginBottom="16dp"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:maxLines="2"
            android:ellipsize="end"
            android:text="Название автомобиля" />

        <!-- Логотип бренда в правом нижнем углу -->
        <ImageView
            android:id="@+id/imageViewBrandLogo"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_alignBottom="@id/imageViewWork"
            android:layout_alignEnd="@id/imageViewWork"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="16dp"
            android:src="@drawable/ic_launcher_foreground"
            android:contentDescription="Логотип бренда" />

    </RelativeLayout>

</androidx.cardview.widget.CardView>